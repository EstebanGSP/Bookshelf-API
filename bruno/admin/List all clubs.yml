info:
  name: List all clubs
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/admin/clubs?page=1&limit=10"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
