info:
  name: Enable user
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{host}}/admin/users/{{userId}}/enable"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
