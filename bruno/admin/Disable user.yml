info:
  name: Disable user
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{host}}/admin/users/{{userId}}/disable"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
