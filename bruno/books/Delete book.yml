info:
  name: Delete book
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{host}}/clubs/{{clubId}}/books/{{bookId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
