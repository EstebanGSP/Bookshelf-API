info:
  name: Delete review
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{host}}/clubs/{{clubId}}/books/{{bookId}}/reviews/{{reviewId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
