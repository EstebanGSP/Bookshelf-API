info:
  name: Export club books
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/clubs/{{clubId}}/exports/books"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
