info:
  name: Remove member
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{host}}/clubs/{{clubId}}/members/{{memberId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
